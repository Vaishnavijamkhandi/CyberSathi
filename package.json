{
  "name": "cybersaathi-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace runner for CyberSaathi",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint"
  }
}
